<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#000000">

    <!-- Message de remerciement -->
    <TextView
        android:id="@+id/tvMerci"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Merci pour votre location !"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp"/>

    <!-- ListView récapitulatif des locations récentes -->
    <ListView
        android:id="@+id/lvRecap"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:divider="#FFFFFF"
        android:dividerHeight="1dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark"
        app:layout_constraintTop_toBottomOf="@id/tvMerci"
        app:layout_constraintBottom_toTopOf="@+id/btnRetourAccueil"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"/>

    <!-- Bouton Retour à la liste des DVD -->
    <Button
        android:id="@+id/btnRetourAccueil"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Retour à la liste des DVD"
        android:backgroundTint="#2196F3"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>
